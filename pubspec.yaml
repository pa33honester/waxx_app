# flutter version : 3.32.5

name: waxxapp
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.1+3

environment:
  sdk: ^3.6.1

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  get: ^4.7.2
  get_storage: ^2.1.1
  loading_animation_widget: ^1.3.0
  google_fonts: 8.0.2
  blurrycontainer: ^2.1.0
  smooth_page_indicator: ^1.2.1
  dotted_line: ^3.2.3
  flutter_rating_bar: ^4.0.1
  pinput: ^5.0.1
  image_picker: ^1.1.2
  http: ^1.4.0
  #  platform_device_id: ^1.0.1
  mobile_device_identifier: ^0.0.3
  intl: ^0.20.2
  fluttertoast: ^8.2.12
  google_sign_in: ^6.3.0
  country_picker: ^2.0.27
  shimmer: ^3.0.0

  #  connectivity: ^3.0.6
  syncfusion_flutter_datepicker: ^30.1.39
  cool_dropdown: ^2.1.1
  video_player: ^2.10.0
  chewie: ^1.12.1
  get_video_thumbnail: ^0.7.0

  #******** Firebase related ********
  firebase_auth: ^5.6.2
  firebase_core: ^3.15.1
  firebase_messaging: ^15.2.9
  flutter_local_notifications: ^19.3.0
  sign_in_with_apple: ^7.0.1
  #******** for zego & socket ********
  zego_express_engine: ^3.21.0
  permission_handler: ^12.0.1
  encrypt: ^5.0.3
  native_device_orientation: ^2.0.3
  socket_io_client: ^3.1.2
  cached_network_image: ^3.4.1
  #  connectivity_plus: ^1.1.0
  razorpay_flutter: ^1.4.0
  path_provider: ^2.1.5
  flutter_stripe: ^11.5.0
  url_launcher: ^6.3.2
  firebase_crashlytics: ^4.3.9
  preload_page_view: ^0.2.0
  vibration: ^3.1.3
  dotted_border: ^3.1.0
  intl_phone_field: ^3.2.0
  dropdown_button2: ^2.3.9
  share_plus: ^11.0.0
  country_code_picker: ^3.3.0
  #******** Now not usable ********
  #  intl_phone_field: ^3.1.0
  #  custom_refresh_indicator: ^2.2.1
  #  email_auth: ^1.0.0
  device_info_plus: ^11.5.0
  flutterwave_standard: ^1.1.0
  flutter_swipe_button: ^2.1.3
  flutter_branch_sdk: ^8.5.0
  flutter_spinkit: ^5.2.1
  swipeable_button_view: ^0.0.2
  slidable_button: ^3.0.0+1
  multi_image_picker_view: ^3.0.0
  file_picker: ^10.2.0
  widget_and_text_animator: ^1.1.5
  slide_action: ^0.0.2
  flutter_bounce: ^1.1.0
  lottie: ^3.3.1
  flutter_html: ^3.0.0
  flutter_flip_card: ^0.0.6
  flutter_sticky_header: ^0.8.0
  connectivity_plus: ^6.1.4
  internet_connection_checker: ^3.0.1
  flutter_launcher_icons: ^0.14.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/Live_page_image/
    - assets/Entry_image/
    - assets/button_image/
    - assets/Home_page_image/
    - assets/Home_page_image/new_collection/
    - assets/Home_page_image/live_selling/
    - assets/Home_page_image/just_for_you/
    - assets/bottombar_image/selected/
    - assets/bottombar_image/unselected/
    - assets/gallary_image/flash_sale/
    - assets/product_detail/
    - assets/product_review/
    - assets/icons/
    - assets/images/
    - assets/profile_icons/
    - assets/gif/
    - assets/data/
    - assets/lottie/
    - assets/no_data_found/
    - assets/FakeItemsForLive/
    - assets/placeholder/
    - assets/lottie/
    - assets/country/

  fonts:
    - family: KurdisRegular
      fonts:
        - asset: assets/fonts/Kurdis Regular.otf

    - family: KurdisSemiBold
      fonts:
        - asset: assets/fonts/Kurdis Semi Bold.otf

    - family: KurdisBold
      fonts:
        - asset: assets/fonts/Kurdis Bold.otf

    - family: KurdisExtraBold
      fonts:
        - asset: assets/fonts/Kurdis Extra Bold.otf



  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

flutter_icons:
  android: true
  ios: true
  image_path: "assets/icons/waxxapp_icon.png" # Path to your icon image
  adaptive_icon_background: "#ffffff"   # (Optional) Background color for adaptive icon on Android
  adaptive_icon_foreground: "assets/icons/waxxapp_icon.png"  # (Optional) Foreground image for adaptive icon
