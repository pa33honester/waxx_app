<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:label="Waxxapp"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!--
                Required for Firebase Phone Auth reCAPTCHA flow.
                After the reCAPTCHA challenge completes in Chrome Custom Tab,
                Firebase redirects back to the app via:
                  com.waxxapp://firebaseauth/link?...
                Without this intent-filter Android cannot route the redirect
                back to the app and the OTP flow stalls on the reCAPTCHA screen.
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="com.waxxapp"/>
            </intent-filter>

            <!--
                Waxxapp App Links — opens https://www.waxxapp.com/short/<id>
                directly in the app once the domain is verified via
                /.well-known/assetlinks.json. Until the SHA-256 fingerprint
                in that file matches the signing cert, the link still works
                as a regular HTTPS URL (falls back to the web preview).
            -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="www.waxxapp.com" android:pathPrefix="/short/" />
                <data android:scheme="https" android:host="waxxapp.com" android:pathPrefix="/short/" />
                <data android:scheme="https" android:host="www.waxxapp.com" android:pathPrefix="/live/" />
                <data android:scheme="https" android:host="waxxapp.com" android:pathPrefix="/live/" />
            </intent-filter>

            <!--
                Custom waxxapp:// scheme used by the web preview's "Open in app"
                button as a fallback path. The Android Intent URL also dispatches
                via this scheme on platforms where the App-Link path can't fire
                (e.g. before the assetlinks.json fingerprint is verified).
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="waxxapp" />
            </intent-filter>

        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->

        <meta-data android:name="io.branch.sdk.BranchKey" android:value="Enter your branch io key" />
        <!-- REPLACE `BranchKey.test` with the value from your Branch Dashboard -->
        <meta-data android:name="io.branch.sdk.BranchKey.test" android:value="Enter your branch io secret key" />

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Force-enable the Impeller renderer on Android. Required for
             LDPlayer / older emulator GPU drivers that crash or render
             black on the legacy Skia OpenGL backend. -->
        <meta-data
            android:name="io.flutter.embedding.android.EnableImpeller"
            android:value="true" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
